<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lms.mapper.LoginLogMapper">

    <!-- 로그인 로그 저장 -->
    <insert id="insertLoginLog">
        INSERT INTO LOGIN_LOG( USER_ID
                             , LOGIN_DT
                             , DEVICE_TYPE
                             , OS_NAME
                             , BROWSER_NAME
                             , USER_AGENT
                             , INS_IP
                             , INS_ID
                             , INS_DT
                             , UPD_IP
                             , UPD_ID
                             , UPD_DT
                            ) VALUES (
                              #{userId}
                            , NOW()
                            , #{deviceType}
                            , #{osName}
                            , #{browserName}
                            , #{userAgent}
                            , #{clientIp}
                            , #{userId}
                            , NOW()
                            , #{clientIp}
                            , #{userId}
                            , NOW()
                            )
    </insert>

</mapper>